# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `npx changeset` to add one when a change affects published behavior; the
release workflow turns accumulated changesets into a version PR and publishes
on merge. See the Development section of the README.
